class ExampleCodec:
    """
    Template feature-layer codec for an interactive widget.

    Put validation and command lowering here when the interactive window emits Python
    or owns domain behavior. If the widget uses a package owned elsewhere, replace
    this with calls into the existing `hyde.features/..._features.py` owner.
    """

    pass
