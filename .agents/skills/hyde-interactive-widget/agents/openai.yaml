display_name: Hyde Interactive Widget
short_description: Create or normalize HydeInteractiveWidget plugins around the shared stable-name and save/restore lifecycle.
default_prompt: Create or normalize a HydeInteractiveWidget-based Hyde plugin so the widget follows the standard base-owned stable naming, save/restore, geometry, and tracked-namespace behavior, with domain logic kept in the feature layer when appropriate.
