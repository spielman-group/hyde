class ExampleCodec:
    """
    Template feature-layer codec for a tool widget.

    Put validation and command lowering here if the tool emits Python or owns domain
    behavior. If the tool uses a package owned elsewhere, replace this with calls into
    the existing `hyde.features/..._features.py` owner instead of duplicating logic.
    """

    pass
